services:
  - type: web
    name: zain-logistics
    runtime: node
    buildCommand: cd client && npm install && npm run build && cd ../server && npm install && npm run seed
    startCommand: cd server && node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
